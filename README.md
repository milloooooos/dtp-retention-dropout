# DTP 患者服务 · 留存率 / 脱落率 自助分析

上传 **销售底表** 与 **随访任务表**，自动计算 DTP 药房患者管理的核心留存/脱落指标，并把"销售算出的脱落患者"与"随访记录的脱落原因"跨表关联，输出可控/不可控构成与行动建议。

> 面向 AZ（阿斯利康）等药企委托 DTP 药房的患者服务复盘场景。数据全程在本地/浏览器处理，不上传第三方。

## 功能

| 模块 | 口径 | 说明 |
|------|------|------|
| **留存率 Cohort（两套口径）** | 按首购月分群（=新患队列） | ①仅看购药时间：Mk=第k月当月有购药占比；②结合说明书盒数覆盖：购药覆盖=盒数×每盒天数，覆盖到第k月即算留存。两套并存，差值≈囤药/断续强度 |
| **脱落率 A（滚动）** | 基准月 M-2 有购药、M-1∪M 无购药 → 脱落 | 月度口径，适合看趋势 |
| **脱落率 B（累计沉默）** | 末次购药距数据终点 > N×用药间隔 → 真停药 | 另给「已观察%」剔除新近患者右删失 |
| **跨表关联** | 销售脱落患者 ↔ 随访脱落原因 | 品牌层面关联（可靠）+ 近似患者级（线索） |
| **脱落映射** | 原因文本 → 可控/不可控 | 关键词分类 + 行动建议映射 |
| **自丰富品牌映射表** | 药名 → 品牌 | 品牌字直匹配 → 通用名别名匹配 → 从数据自动学习新别名写回 `brand_aliases.json`；未识别记入 unresolved 供人工补录 |

## 快速开始（本地）

```bash
pip install -r requirements.txt
streamlit run app.py
# 浏览器打开 http://localhost:8501
```

1. 上传「销售底表」（xlsx/xls）
2. 可选上传「随访任务表」（可多选 2025H1 + 2026H1 两个 xls）
3. 调侧边栏参数（留存追踪月数、脱落率B倍数、是否输出近似患者级）
4. 点「运行分析」→ 看图表/表格 → 点「下载完整 Excel 报告」

> 不传随访表也能算留存率与脱落率，只是没有跨表脱落原因。

## 部署到 Streamlit Cloud（免服务器、自己上传数据）

1. 把本仓库推到你的 GitHub（见下）。
2. 打开 https://streamlit.io/cloud → 用 GitHub 登录 → New app → 选本仓库 → 入口 `app.py`。
3. 部署完成后，别人（或你）打开链接即可上传自己的数据自动分析。数据不离开浏览器。

## 推送到 GitHub

```bash
git init
git add app.py dtp_engine.py run_dtp.py requirements.txt README.md .gitignore
git commit -m "DTP 留存率/脱落率 自助分析 Streamlit app"
git branch -M main
git remote add origin https://github.com/<你的用户名>/<仓库名>.git
git push -u origin main
```

> 若本机没登录 GitHub：`gh auth login` 后重试；或在 GitHub 网页新建空仓库，再按上面 remote/add + push。

## 数据要求

**销售底表**（Sheet 含以下列，列名可用等价别名，引擎自动识别）：
`商品名称`（含品牌括号，如"甲磺酸奥希替尼片(泰瑞沙)"）、`销售时间`、`销售数量`、`患者ID`（或会员号/开票抬头）、`会员姓名`（可选）、`药房名称`、`医疗单位`、`处方医生`。

**随访任务表**（xls，历史任务导出）：
`药品名称`、`患者`（姓名）、`用药周期状态`、`未按计划持续用药原因`、`本月未购药的原因`、`患者首购日期`、`末次购药日期`、`创建日期`、`任务类型`。

> 支持品牌：泰瑞沙 / 优赫得 / 利普卓 / 英飞凡 / 沃瑞沙 / 凡舒卓 / 荃科得。用药间隔（每盒天数）内置，可在 `dtp_engine.DAYS_PER_BOX` 调整。

## 重要口径边界

- **留存率两套口径**：口径1（仅购药时间）会因一次购多盒、次月不买而非单调递减（真实特征）；口径2（盒数覆盖）把囤药者的在治期计入，更平滑单调、更贴近真实在治率。近期 cohort 窗口不足显示为空（右删失）。
- **品牌自丰富映射**：`brand_aliases.json` 随每次上传自动学习新「通用名→品牌」并落盘；即便未来文件只给通用名（无品牌字）也能识别。识别不出的写入 `unresolved` 字段，可手动补进 `aliases`。
- **两表无共同患者主键**：销售表会员号（浮点格式）与随访表患者编码/会员号（字符串）格式不同，无法直接对患者关联。故：
  - **品牌层面关联为主**（可靠）：各品种销售脱落率 ↔ 各品种随访原因可控占比。
  - **近似患者级关联**（置信度低）：按 姓名+品种+首购月 匹配，同名歧义大，仅作线索排查。
- **随访覆盖缺口**：若随访表某品种为 0 行（如本例优赫得），跨表该品种原因为空，属覆盖缺口非计算错误。
- **脱落率 B 右删失**：新近患者（首购在终点前 N×间隔内）未到判断停药的时机，已用「已观察%」剔除。

## 文件结构

```
app.py             # Streamlit 自助分析 Web App（入口）
dtp_engine.py      # 分析引擎（留存×2/脱落A/B/跨表关联/脱落映射/自丰富品牌映射），可被 App 与脚本复用
run_dtp.py         # 本地批量运行：读固定路径数据 → 输出 Excel（便于无界面跑全量）
brand_aliases.json # 品牌别名映射表（通用名→品牌），随运行自动丰富
requirements.txt   # 依赖
README.md          # 本文档
```
